<nav class="pagination" role="navigation">
  {{#if prev}}
  <a class="posts newer btn small" href="{{page_url prev}}#open">&larr; Posts mais recentes</a>
  {{/if}}
  <span class="posts index">Página {{page}} de {{pages}}</span>
  {{#if next}}
 <a class="posts older btn small" href="{{page_url next}}#open">Posts mais antigos &rarr;</a>
  {{/if}}
</nav>
